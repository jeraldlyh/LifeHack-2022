import { Text } from "react-native";
import { Container } from "../../common/components";

export const LoginScreen = () => {
    return (
        <Container>
            <Text>login</Text>
        </Container>
    );
};
